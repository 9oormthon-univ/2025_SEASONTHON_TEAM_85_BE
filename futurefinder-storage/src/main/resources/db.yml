spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/futurefinder
    username: root
    password: tjljh6767.

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update # update, create
    properties:
      hibernate:
        format_sql: true   # ✅ 이렇게!